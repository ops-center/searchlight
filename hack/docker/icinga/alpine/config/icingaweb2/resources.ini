[icingaweb_db]
type = "db"
db = "pgsql"
host = "$ICINGA_WEB_HOST"
port = "$ICINGA_WEB_PORT"
dbname = "$ICINGA_WEB_DB"
username = "$ICINGA_WEB_USER"
password = "$ICINGA_WEB_PASSWORD"

[icinga_ido]
type = "db"
db = "pgsql"
host = "$ICINGA_IDO_HOST"
port = "$ICINGA_IDO_PORT"
dbname = "$ICINGA_IDO_DB"
username = "$ICINGA_IDO_USER"
password = "$ICINGA_IDO_PASSWORD"
